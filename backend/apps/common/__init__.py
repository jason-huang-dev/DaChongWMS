"""Shared helpers for first-class backend apps.

This package is intentionally small. Reusable helpers that apply across the
modular `apps.*` backend can live here, but app-specific logic should stay in
the owning app instead of turning this into another catch-all utils module.
"""
